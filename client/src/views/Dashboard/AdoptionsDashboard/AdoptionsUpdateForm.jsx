import { useParams } from "react-router-dom";

const AdoptionsUpdateForm = ()=>{
    const { id } = useParams();
    return(
        <form>
            <h1>Form</h1>

        </form>
    )
};

export default AdoptionsUpdateForm;