import { useParams } from "react-router-dom";

const RescuesUpdateForm = ()=>{
    const { id } = useParams();
    return(
        <form>
            <h1>Form</h1>

        </form>
    )
};

export default RescuesUpdateForm;